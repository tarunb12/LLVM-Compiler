// if/else test
int main() {
    if (0 > 1) {
        printf("%d > %d\n", 0, 1)
        printf("(%s)", "if block")
    } else if (1 < 2) {
        printf("%d < %d\n", 1, 2)
        printf("(%s)", "else if block")
    }
    return 0
}