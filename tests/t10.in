// break test

int main() {
    int i = 6
    int j = 0
    while (j < i) {
        if (j == 4) break
        printf("%d %s\n", j++, "iteration (no breaks yet)")
    }
    printf("it broke")
    return 0
}