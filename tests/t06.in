// for loop test
int main() {
    int max = 5
    int i
    for (i = 0; i <= max; i++) {
        if (i == 1) printf("%d step\n", i)
        else if (i < max) printf("%d steps\n", i)
        else printf ("%dth and final step", i)
    }
    return 0
}